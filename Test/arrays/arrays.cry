def main()
  print([1, 2, 3,
         4, 5, 6,
		 7, 8, 9])
  
  val = "Crystal"
  val += " " + "Clear"
  x = [2, nil, true, "Hello", 6.10, nil, val]
  x[1] = [[true, [val, "Hello"]], 3]
  print_color("Array X:" _
			 , 11) 
  print(x)
  print("Size: " + size(x) + "\n")

  #unrolling an array
  print_color("Unroll X:", 11)

  index = 0
  while(index < size(x))
   print(x[index])
   index += 1
  end

  print("")

  y = 10 ... 100.0 / 6.3 + 5
  print_color("Range Y:", 11) 
  print(y)
  print("Size: " + size(y) + "\n")

  #unrolling an array
  print_color("Unroll Y + 10:", 11) 
  
  #index = 0
  #while(index < size(y))
  # print(y[index] + 10)
  # index += 1
  #end

  return x
end